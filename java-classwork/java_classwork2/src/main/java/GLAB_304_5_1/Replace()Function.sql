-- UPDATE tbl_name SET
-- field_name = REPLACE(field_name, string_to_find, string_to_replace)
       --          WHERE conditions;

-- UPDATE products
-- SET productDescription = REPLACE(productDescription,'abuot','about');
-- The above query will find all occurrences of a spelling mistake ’abuot,’ and replaces it
-- by the correct word ‘about’ in the productDescription column of the products table.